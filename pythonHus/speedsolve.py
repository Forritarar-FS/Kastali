import turtle

speedscreen = turtle.Screen()	
speedsolve = turtle.Turtle()

speedsolve.speed(1)

speedsolve.forward(256)
speedsolve.right(128)
speedsolve.forward(64)
speedsolve.left(32)
speedsolve.backward(16)
